pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello build'
            }
        }
        stage('test') {
            steps {
                echo 'test'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy'
            }   
        }
    }
}
